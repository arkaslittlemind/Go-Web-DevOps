apiVersion: v1
kind: Service
metadata:
  name: go-web-devops
  labels:
    app: go-web-devops
spec:
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
  selector:
    app: go-web-devops
  type: ClusterIP
